import { JSX } from "preact";
import { UploadWidgetConfigRequired } from "../../../../../config/UploadWidgetConfig";
import { UploadedFile } from "../../../../../modules/UploadedFile";
import { UploadTracker } from "../../../../../modules/UploadTracker";
interface Props {
    imageCount: number;
    imageIndex: number;
    onImageEdited: (keep: boolean, editedFile: UploadedFile | undefined) => void;
    options: UploadWidgetConfigRequired;
    originalImage: UploadedFile;
    upload: UploadTracker;
}
export declare const ImageEditor: ({ imageCount, imageIndex, originalImage, upload, onImageEdited, options }: Props) => JSX.Element;
export {};
